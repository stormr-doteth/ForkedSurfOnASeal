-- MasterScript shim for Time Trials
-- EndPlatform's EndTriggerScript expects game.ServerScriptService.MasterScript.PlayerCompleted
-- This empty module makes the folder exist so Rojo creates it as a ModuleScript with children.
return nil
